<UserControl x:Class="InfraScheduler.Views.ToolView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:InfraScheduler.Views"
             xmlns:viewmodels="clr-namespace:InfraScheduler.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewmodels:ToolViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Search Bar -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
            <TextBox Width="300" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0,0,10,0" Padding="5"/>
            <Button Content="Search" Command="{Binding SearchToolsCommand}" Padding="10,5"/>
        </StackPanel>

        <!-- Main Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="400"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Form Panel -->
            <StackPanel Grid.Column="0" Margin="0,0,20,0">
                <TabControl>
                    <TabItem Header="Tool Addition">
                        <StackPanel>
                            <GroupBox Header="Tool Details" Padding="10">
                                <StackPanel>
                                    <TextBlock Text="Item Type:" Margin="0,5"/>
                                    <TextBox Text="{Binding ItemType}" Margin="0,5"/>
                                    
                                    <TextBlock Text="Description:" Margin="0,5"/>
                                    <TextBox Text="{Binding Description}" Margin="0,5"/>
                                    
                                    <TextBlock Text="Quantity:" Margin="0,5"/>
                                    <TextBox Text="{Binding Quantity}" Margin="0,5"/>
                                    
                                    <TextBlock Text="Condition:" Margin="0,5"/>
                                    <ComboBox Text="{Binding Condition}" Height="30" Margin="0,5">
                                        <ComboBoxItem Content="New"/>
                                        <ComboBoxItem Content="Good"/>
                                        <ComboBoxItem Content="Fair"/>
                                        <ComboBoxItem Content="Poor"/>
                                    </ComboBox>
                                    
                                    <StackPanel Orientation="Horizontal" Margin="0,10">
                                        <Button Content="Add Batch" Command="{Binding AddToolCommand}" Margin="0,0,10,0" Padding="20,5"/>
                                        <Button Content="Update" Command="{Binding UpdateToolCommand}" Margin="0,0,10,0" Padding="20,5"/>
                                        <Button Content="Delete" Command="{Binding DeleteToolCommand}" Padding="20,5"/>
                                    </StackPanel>
                                </StackPanel>
                            </GroupBox>

                            <!-- Item Types Panel -->
                        </StackPanel>
                    </TabItem>
                    
                    <TabItem Header="Tool Assignment">
                        <StackPanel>
                            <!-- Tool Selection Section -->
                            <GroupBox Header="Select Tool" Padding="10" Margin="0,0,0,10">
                                <StackPanel>
                                    <TextBlock Text="Available Tools:" Margin="0,5"/>
                                    <ComboBox ItemsSource="{Binding Tools}"
                                              SelectedItem="{Binding SelectedTool}"
                                              DisplayMemberPath="ModelNumber"
                                              Margin="0,5"
                                              Height="30"/>
                                    
                                    <TextBlock Text="Selected Tool Details:" Margin="0,10,0,5" FontWeight="Bold"/>
                                    <StackPanel Orientation="Horizontal" Margin="0,5">
                                        <TextBlock Text="Item Type: " FontWeight="SemiBold"/>
                                        <TextBlock Text="{Binding SelectedTool.Name}" Margin="5,0,0,0"/>
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal" Margin="0,5">
                                        <TextBlock Text="Model Number: " FontWeight="SemiBold"/>
                                        <TextBlock Text="{Binding SelectedTool.ModelNumber}" Margin="5,0,0,0"/>
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal" Margin="0,5">
                                        <TextBlock Text="Status: " FontWeight="SemiBold"/>
                                        <TextBlock Text="{Binding SelectedTool.Status}" Margin="5,0,0,0"/>
                                    </StackPanel>
                                </StackPanel>
                            </GroupBox>

                            <!-- Assignment Section -->
                            <GroupBox Header="Tool Assignment" Padding="10">
                                <StackPanel>
                                    <TextBlock Text="Technician:" Margin="0,5"/>
                                    <ComboBox ItemsSource="{Binding Technicians}"
                                              SelectedValue="{Binding SelectedTechnicianId}"
                                              SelectedValuePath="Id"
                                              DisplayMemberPath="FullName"
                                              Margin="0,5"
                                              Height="30"/>
                                    
                                    <TextBlock Text="Job:" Margin="0,5"/>
                                    <ComboBox ItemsSource="{Binding Jobs}"
                                              SelectedValue="{Binding SelectedJobId}"
                                              SelectedValuePath="Id"
                                              DisplayMemberPath="Name"
                                              Margin="0,5"
                                              Height="30"/>
                                    
                                    <TextBlock Text="Checkout Date:" Margin="0,5"/>
                                    <DatePicker SelectedDate="{Binding CheckoutDate}" Margin="0,5"/>
                                    
                                    <TextBlock Text="Expected Return Date:" Margin="0,5"/>
                                    <DatePicker SelectedDate="{Binding ExpectedReturnDate}" Margin="0,5"/>
                                    
                                    <StackPanel Orientation="Horizontal" Margin="0,10">
                                        <Button Content="Checkout" Command="{Binding CheckoutToolCommand}" Margin="0,0,10,0" Padding="20,5"/>
                                        <Button Content="Return" Command="{Binding ReturnToolCommand}" Padding="20,5"/>
                                    </StackPanel>
                                </StackPanel>
                            </GroupBox>
                        </StackPanel>
                    </TabItem>
                    
                    <TabItem Header="Tool Maintenance">
                        <StackPanel>
                            <!-- Tool Selection Section -->
                            <GroupBox Header="Select Tool" Padding="10" Margin="0,0,0,10">
                                <StackPanel>
                                    <TextBlock Text="Available Tools:" Margin="0,5"/>
                                    <ComboBox ItemsSource="{Binding Tools}"
                                              SelectedItem="{Binding SelectedTool}"
                                              DisplayMemberPath="ModelNumber"
                                              Margin="0,5"
                                              Height="30"/>
                                    
                                    <TextBlock Text="Selected Tool Details:" Margin="0,10,0,5" FontWeight="Bold"/>
                                    <StackPanel Orientation="Horizontal" Margin="0,5">
                                        <TextBlock Text="Item Type: " FontWeight="SemiBold"/>
                                        <TextBlock Text="{Binding SelectedTool.Name}" Margin="5,0,0,0"/>
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal" Margin="0,5">
                                        <TextBlock Text="Model Number: " FontWeight="SemiBold"/>
                                        <TextBlock Text="{Binding SelectedTool.ModelNumber}" Margin="5,0,0,0"/>
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal" Margin="0,5">
                                        <TextBlock Text="Status: " FontWeight="SemiBold"/>
                                        <TextBlock Text="{Binding SelectedTool.Status}" Margin="5,0,0,0"/>
                                    </StackPanel>
                                </StackPanel>
                            </GroupBox>

                            <!-- Maintenance Section -->
                            <GroupBox Header="Tool Maintenance" Padding="10">
                                <StackPanel>
                                    <TextBlock Text="Maintenance Date:" Margin="0,5"/>
                                    <DatePicker SelectedDate="{Binding MaintenanceDate}" Margin="0,5"/>
                                    
                                    <TextBlock Text="Maintenance Type:" Margin="0,5"/>
                                    <ComboBox Text="{Binding MaintenanceType}" Margin="0,5" Height="30">
                                        <ComboBoxItem Content="Regular"/>
                                        <ComboBoxItem Content="Repair"/>
                                        <ComboBoxItem Content="Calibration"/>
                                    </ComboBox>
                                    
                                    <TextBlock Text="Performed By:" Margin="0,5"/>
                                    <TextBox Text="{Binding PerformedBy}" Margin="0,5"/>
                                    
                                    <Button Content="Add Maintenance" Command="{Binding AddMaintenanceCommand}" Margin="0,10" Padding="20,5"/>
                                </StackPanel>
                            </GroupBox>
                        </StackPanel>
                    </TabItem>
                </TabControl>
            </StackPanel>

            <!-- Data Grid -->
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <DataGrid ItemsSource="{Binding Tools}"
                          SelectedItem="{Binding SelectedTool}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          GridLinesVisibility="All"
                          BorderBrush="#DDDDDD"
                          BorderThickness="1"
                          ScrollViewer.VerticalScrollBarVisibility="Auto"
                          ScrollViewer.HorizontalScrollBarVisibility="Auto"
                          MaxHeight="400">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Item Type" Binding="{Binding Name}" Width="150"/>
                        <DataGridTextColumn Header="Model Number" Binding="{Binding ModelNumber}" Width="120"/>
                        <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100"/>
                        <DataGridTextColumn Header="Condition" Binding="{Binding Condition}" Width="100"/>
                        <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="200"/>
                        <DataGridTextColumn Header="Location" Binding="{Binding CurrentLocation}" Width="150"/>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- Assignment and Maintenance History -->
                <TabControl Grid.Row="1" Margin="0,20,0,0">
                    <TabItem Header="Assignments">
                        <DataGrid ItemsSource="{Binding Assignments}"
                                  AutoGenerateColumns="False"
                                  IsReadOnly="True"
                                  GridLinesVisibility="All"
                                  BorderBrush="#DDDDDD"
                                  BorderThickness="1"
                                  Height="200">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Technician" Binding="{Binding Technician.FullName}" Width="150"/>
                                <DataGridTextColumn Header="Job" Binding="{Binding Job.Name}" Width="150"/>
                                <DataGridTextColumn Header="Checkout Date" Binding="{Binding CheckoutDate, StringFormat=d}" Width="120"/>
                                <DataGridTextColumn Header="Expected Return" Binding="{Binding ExpectedReturnDate, StringFormat=d}" Width="120"/>
                                <DataGridTextColumn Header="Actual Return" Binding="{Binding ActualReturnDate, StringFormat=d}" Width="120"/>
                                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </TabItem>
                    <TabItem Header="Maintenance History">
                        <DataGrid ItemsSource="{Binding MaintenanceHistory}"
                                  AutoGenerateColumns="False"
                                  IsReadOnly="True"
                                  GridLinesVisibility="All"
                                  BorderBrush="#DDDDDD"
                                  BorderThickness="1"
                                  Height="200">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Date" Binding="{Binding MaintenanceDate, StringFormat=d}" Width="100"/>
                                <DataGridTextColumn Header="Type" Binding="{Binding MaintenanceType}" Width="100"/>
                                <DataGridTextColumn Header="Performed By" Binding="{Binding PerformedBy}" Width="150"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </TabItem>
                </TabControl>
            </Grid>
        </Grid>
    </Grid>
</UserControl> 