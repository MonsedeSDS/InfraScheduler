<UserControl x:Class="InfraScheduler.Views.SubcontractorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             FontFamily="Segoe UI">

    <Grid Background="#FAFAFA">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Windows 10 Header with Navigation Path -->
        <Border Grid.Row="0" Background="White" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1" Padding="24,16,24,16">
            <StackPanel>
                <!-- Navigation Breadcrumb -->
                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                    <TextBlock Text="Home" Foreground="#0078D4" FontSize="14" FontWeight="Normal"/>
                    <TextBlock Text=" > " Foreground="#666666" FontSize="14" Margin="8,0,8,0"/>
                    <TextBlock Text="Teams" Foreground="#666666" FontSize="14" Margin="0,0,8,0"/>
                    <TextBlock Text=" > " Foreground="#666666" FontSize="14" Margin="0,0,8,0"/>
                    <TextBlock Text="Subcontractors" Foreground="#666666" FontSize="14" FontWeight="Normal"/>
                </StackPanel>
                
                <!-- Main Heading -->
                <TextBlock Text="Subcontractor Management" FontSize="32" FontWeight="Light" Foreground="#333333"/>
                
                <!-- Subtitle -->
                <TextBlock Text="Manage subcontractor companies and contact information" FontSize="16" Foreground="#666666" Margin="0,8,0,0"/>
            </StackPanel>
        </Border>

        <!-- Content Area -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="24">
            <StackPanel>
                <!-- Search Bar -->
                <Border Background="White" CornerRadius="4" Padding="24" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="Search Subcontractors" FontSize="20" FontWeight="SemiBold" Foreground="#333333" Margin="0,0,0,16"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Search Company:" VerticalAlignment="Center" Margin="0,0,10,0" FontSize="14" FontWeight="Medium" Foreground="#333333"/>
                            <TextBox Grid.Column="1" Text="{Binding CompanyName, UpdateSourceTrigger=PropertyChanged}" 
                                     Padding="12,8" VerticalContentAlignment="Center" Height="36"
                                     Background="White" BorderBrush="#E0E0E0" BorderThickness="1" FontSize="14"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Main Content -->
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="350"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Form Panel -->
                    <Border Grid.Column="0" Background="White" CornerRadius="4" Padding="24" Margin="0,0,16,0">
                        <StackPanel>
                            <TextBlock Text="Subcontractor Details" FontSize="20" FontWeight="SemiBold" Foreground="#333333" Margin="0,0,0,16"/>
                            
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <TextBlock Text="Company Name:" Grid.Row="0" Margin="0,0,0,5" FontSize="14" FontWeight="Medium" Foreground="#333333"/>
                                <TextBox Grid.Row="1" Text="{Binding CompanyName, UpdateSourceTrigger=PropertyChanged}" 
                                         Margin="0,0,0,10" Padding="12,8" Height="36"
                                         Background="White" BorderBrush="#E0E0E0" BorderThickness="1" FontSize="14"/>

                                <TextBlock Text="Contact Person:" Grid.Row="2" Margin="0,0,0,5" FontSize="14" FontWeight="Medium" Foreground="#333333"/>
                                <TextBox Grid.Row="3" Text="{Binding ContactPerson, UpdateSourceTrigger=PropertyChanged}" 
                                         Margin="0,0,0,10" Padding="12,8" Height="36"
                                         Background="White" BorderBrush="#E0E0E0" BorderThickness="1" FontSize="14"/>

                                <TextBlock Text="Phone:" Grid.Row="4" Margin="0,0,0,5" FontSize="14" FontWeight="Medium" Foreground="#333333"/>
                                <TextBox Grid.Row="5" Text="{Binding Phone, UpdateSourceTrigger=PropertyChanged}" 
                                         Margin="0,0,0,10" Padding="12,8" Height="36"
                                         Background="White" BorderBrush="#E0E0E0" BorderThickness="1" FontSize="14"/>

                                <TextBlock Text="Email:" Grid.Row="6" Margin="0,0,0,5" FontSize="14" FontWeight="Medium" Foreground="#333333"/>
                                <TextBox Grid.Row="7" Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" 
                                         Margin="0,0,0,10" Padding="12,8" Height="36"
                                         Background="White" BorderBrush="#E0E0E0" BorderThickness="1" FontSize="14"/>

                                <TextBlock Text="Address:" Grid.Row="8" Margin="0,0,0,5" FontSize="14" FontWeight="Medium" Foreground="#333333"/>
                                <TextBox Grid.Row="9" Text="{Binding Address, UpdateSourceTrigger=PropertyChanged}" 
                                         Margin="0,0,0,10" Padding="12,8" Height="36"
                                         Background="White" BorderBrush="#E0E0E0" BorderThickness="1" FontSize="14"/>

                                <TextBlock Text="Notes:" Grid.Row="10" Margin="0,0,0,5" FontSize="14" FontWeight="Medium" Foreground="#333333"/>
                                <TextBox Grid.Row="11" Text="{Binding Notes, UpdateSourceTrigger=PropertyChanged}" 
                                         Margin="0,0,0,10" Padding="12,8" Height="60" TextWrapping="Wrap" AcceptsReturn="True" 
                                         VerticalScrollBarVisibility="Auto"
                                         Background="White" BorderBrush="#E0E0E0" BorderThickness="1" FontSize="14"/>

                                <StackPanel Grid.Row="13" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
                                    <Button Width="100" Content="Add" Margin="0,0,10,0" Command="{Binding AddSubcontractorCommand}" 
                                            Padding="16,8" Height="36"
                                            Background="#0078D4" Foreground="White" BorderThickness="0" FontSize="14"/>
                                    <Button Width="100" Content="Update" Margin="0,0,10,0" Command="{Binding UpdateSubcontractorCommand}" 
                                            Padding="16,8" Height="36"
                                            Background="White" Foreground="#0078D4" BorderBrush="#0078D4" BorderThickness="1" FontSize="14"/>
                                    <Button Width="100" Content="Delete" Command="{Binding DeleteSubcontractorCommand}" 
                                            Padding="16,8" Height="36"
                                            Background="#E81123" Foreground="White" BorderThickness="0" FontSize="14"/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <!-- DataGrid -->
                    <Border Grid.Column="1" Background="White" CornerRadius="4" Padding="24">
                        <DataGrid ItemsSource="{Binding Subcontractors}"
                                  SelectedItem="{Binding SelectedSubcontractor, Mode=TwoWay}"
                                  AutoGenerateColumns="False"
                                  IsReadOnly="True"
                                  CanUserAddRows="False"
                                  CanUserDeleteRows="False"
                                  CanUserReorderColumns="True"
                                  CanUserResizeColumns="True"
                                  CanUserSortColumns="True"
                                  GridLinesVisibility="Horizontal"
                                  HorizontalGridLinesBrush="#E0E0E0"
                                  VerticalScrollBarVisibility="Auto"
                                  HorizontalScrollBarVisibility="Auto"
                                  MinRowHeight="30"
                                  Background="White" BorderBrush="#E0E0E0" BorderThickness="1">
                            <DataGrid.RowStyle>
                                <Style TargetType="DataGridRow">
                                    <Setter Property="MinHeight" Value="30"/>
                                </Style>
                            </DataGrid.RowStyle>
                            <DataGrid.ColumnHeaderStyle>
                                <Style TargetType="DataGridColumnHeader">
                                    <Setter Property="Background" Value="#F5F5F5"/>
                                    <Setter Property="Foreground" Value="#333333"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                    <Setter Property="Padding" Value="12,8"/>
                                    <Setter Property="BorderBrush" Value="#E0E0E0"/>
                                    <Setter Property="BorderThickness" Value="0,0,1,0"/>
                                </Style>
                            </DataGrid.ColumnHeaderStyle>
                            <DataGrid.CellStyle>
                                <Style TargetType="DataGridCell">
                                    <Setter Property="Padding" Value="12,8"/>
                                    <Setter Property="BorderBrush" Value="#E0E0E0"/>
                                    <Setter Property="BorderThickness" Value="0,0,1,1"/>
                                </Style>
                            </DataGrid.CellStyle>
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Company Name" Binding="{Binding CompanyName}" Width="200">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="TextWrapping" Value="Wrap"/>
                                            <Setter Property="Padding" Value="12,8"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="Contact" Binding="{Binding ContactPerson}" Width="150">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="TextWrapping" Value="Wrap"/>
                                            <Setter Property="Padding" Value="12,8"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="Phone" Binding="{Binding Phone}" Width="120">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="TextWrapping" Value="Wrap"/>
                                            <Setter Property="Padding" Value="12,8"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="200">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="TextWrapping" Value="Wrap"/>
                                            <Setter Property="Padding" Value="12,8"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="Address" Binding="{Binding Address}" Width="200">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="TextWrapping" Value="Wrap"/>
                                            <Setter Property="Padding" Value="12,8"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="Notes" Binding="{Binding Notes}" Width="200">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="TextWrapping" Value="Wrap"/>
                                            <Setter Property="Padding" Value="12,8"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>
                </Grid>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
