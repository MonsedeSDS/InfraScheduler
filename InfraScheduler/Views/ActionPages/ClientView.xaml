<UserControl x:Class="InfraScheduler.Views.ClientView"
           xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
           xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
           xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
           xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
           mc:Ignorable="d"
           FontFamily="Segoe UI">

    <Grid Background="#FAFAFA">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Windows 10 Header with Navigation Path -->
        <Border Grid.Row="0" Background="White" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1" Padding="24,16,24,16">
            <StackPanel>
                <!-- Navigation Breadcrumb -->
                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                    <TextBlock Text="Home" Foreground="#0078D4" FontSize="14" FontWeight="Normal"/>
                    <TextBlock Text=" > " Foreground="#666666" FontSize="14" Margin="8,0,8,0"/>
                    <TextBlock Text="Database" Foreground="#666666" FontSize="14" Margin="0,0,8,0"/>
                    <TextBlock Text=" > " Foreground="#666666" FontSize="14" Margin="0,0,8,0"/>
                    <TextBlock Text="Clients" Foreground="#666666" FontSize="14" FontWeight="Normal"/>
                </StackPanel>
                <!-- Main Heading -->
                <TextBlock Text="Client Management" FontSize="32" FontWeight="Light" Foreground="#333333"/>
                <!-- Subtitle -->
                <TextBlock Text="Manage client records and contact information" FontSize="16" Foreground="#666666" Margin="0,8,0,0"/>
            </StackPanel>
        </Border>

        <!-- Content Area -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="24">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="400"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Form Panel -->
                <Border Grid.Column="0" Background="White" CornerRadius="4" Padding="24" Margin="0,0,16,0">
                    <StackPanel>
                        <!-- Search Bar -->
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                            <TextBox Width="220" Text="{Binding SearchTerm, UpdateSourceTrigger=PropertyChanged}" 
                                     Background="White" BorderBrush="#E0E0E0" BorderThickness="1" 
                                     Padding="12,8" Margin="0,0,12,0" VerticalAlignment="Center" FontSize="14"/>
                        </StackPanel>
                        <TextBlock Text="Client Details" FontSize="18" FontWeight="SemiBold" Foreground="#333333" Margin="0,0,0,16"/>
                        <TextBlock Text="Name:" FontSize="14" FontWeight="Medium" Foreground="#333333" Margin="0,8,0,4"/>
                        <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" 
                                 Background="White" BorderBrush="#E0E0E0" BorderThickness="1" 
                                 Padding="12,8" Height="36" Margin="0,4,0,12" FontSize="14"/>
                        <TextBlock Text="Contact Person:" FontSize="14" FontWeight="Medium" Foreground="#333333" Margin="0,8,0,4"/>
                        <TextBox Text="{Binding ContactPerson, UpdateSourceTrigger=PropertyChanged}" 
                                 Background="White" BorderBrush="#E0E0E0" BorderThickness="1" 
                                 Padding="12,8" Height="36" Margin="0,4,0,12" FontSize="14"/>
                        <TextBlock Text="Phone:" FontSize="14" FontWeight="Medium" Foreground="#333333" Margin="0,8,0,4"/>
                        <TextBox Text="{Binding Phone, UpdateSourceTrigger=PropertyChanged}" 
                                 Background="White" BorderBrush="#E0E0E0" BorderThickness="1" 
                                 Padding="12,8" Height="36" Margin="0,4,0,12" FontSize="14"/>
                        <TextBlock Text="Email:" FontSize="14" FontWeight="Medium" Foreground="#333333" Margin="0,8,0,4"/>
                        <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" 
                                 Background="White" BorderBrush="#E0E0E0" BorderThickness="1" 
                                 Padding="12,8" Height="36" Margin="0,4,0,12" FontSize="14"/>
                        <TextBlock Text="Address:" FontSize="14" FontWeight="Medium" Foreground="#333333" Margin="0,8,0,4"/>
                        <TextBox Text="{Binding Address, UpdateSourceTrigger=PropertyChanged}" 
                                 Background="White" BorderBrush="#E0E0E0" BorderThickness="1" 
                                 Padding="12,8" Height="36" Margin="0,4,0,12" FontSize="14"/>
                        <TextBlock Text="Notes:" FontSize="14" FontWeight="Medium" Foreground="#333333" Margin="0,8,0,4"/>
                        <TextBox Text="{Binding Notes, UpdateSourceTrigger=PropertyChanged}" 
                                 Background="White" BorderBrush="#E0E0E0" BorderThickness="1" 
                                 Padding="12,8" Height="60" Margin="0,4,0,12" FontSize="14" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"/>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
                            <Button Content="Add" Command="{Binding AddClientCommand}" 
                                    Background="#0078D4" Foreground="White" 
                                    Padding="16,8" BorderThickness="0" Margin="0,0,8,0" FontSize="14" Width="100"/>
                            <Button Content="Update" Command="{Binding UpdateClientCommand}" 
                                    Background="White" Foreground="#0078D4" 
                                    Padding="16,8" BorderBrush="#0078D4" BorderThickness="1" 
                                    Margin="0,0,8,0" FontSize="14" Width="100"/>
                            <Button Content="Delete" Command="{Binding DeleteClientCommand}" 
                                    Background="#E81123" Foreground="White" 
                                    Padding="16,8" BorderThickness="0" FontSize="14" Width="100"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- DataGrid Panel -->
                <Border Grid.Column="1" Background="White" CornerRadius="4" Padding="24">
                    <DataGrid ItemsSource="{Binding Clients}"
                              SelectedItem="{Binding SelectedClient, Mode=TwoWay}"
                              AutoGenerateColumns="False"
                              IsReadOnly="True"
                              CanUserAddRows="False"
                              CanUserDeleteRows="False"
                              CanUserReorderColumns="True"
                              CanUserResizeColumns="True"
                              CanUserSortColumns="True"
                              GridLinesVisibility="Horizontal"
                              BorderThickness="0"
                              HorizontalGridLinesBrush="#E0E0E0"
                              VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Auto"
                              MinRowHeight="30"
                              Background="White" BorderBrush="#E0E0E0">
                        <DataGrid.RowStyle>
                            <Style TargetType="DataGridRow">
                                <Setter Property="MinHeight" Value="30"/>
                            </Style>
                        </DataGrid.RowStyle>
                        <DataGrid.ColumnHeaderStyle>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="#F5F5F5"/>
                                <Setter Property="Foreground" Value="#333333"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="Padding" Value="12,8"/>
                                <Setter Property="BorderBrush" Value="#E0E0E0"/>
                                <Setter Property="BorderThickness" Value="0,0,1,0"/>
                            </Style>
                        </DataGrid.ColumnHeaderStyle>
                        <DataGrid.CellStyle>
                            <Style TargetType="DataGridCell">
                                <Setter Property="Padding" Value="12,8"/>
                                <Setter Property="BorderBrush" Value="#E0E0E0"/>
                                <Setter Property="BorderThickness" Value="0,0,1,1"/>
                            </Style>
                        </DataGrid.CellStyle>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="150">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="TextWrapping" Value="Wrap"/>
                                        <Setter Property="Padding" Value="5"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="Contact" Binding="{Binding ContactPerson}" Width="150">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="TextWrapping" Value="Wrap"/>
                                        <Setter Property="Padding" Value="5"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="Phone" Binding="{Binding Phone}" Width="120">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="TextWrapping" Value="Wrap"/>
                                        <Setter Property="Padding" Value="5"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="200">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="TextWrapping" Value="Wrap"/>
                                        <Setter Property="Padding" Value="5"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="Address" Binding="{Binding Address}" Width="200">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="TextWrapping" Value="Wrap"/>
                                        <Setter Property="Padding" Value="5"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="Notes" Binding="{Binding Notes}" Width="200">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="TextWrapping" Value="Wrap"/>
                                        <Setter Property="Padding" Value="5"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
            </Grid>
        </ScrollViewer>
    </Grid>
</UserControl>
