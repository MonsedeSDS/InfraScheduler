<UserControl x:Class="InfraScheduler.Views.JobAcceptanceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#2196F3" Padding="15">
            <TextBlock Text="Job Acceptance - Equipment Batch Creation" 
                       Foreground="White" FontSize="18" FontWeight="Bold"/>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Row="1" Margin="15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="350"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Job Selection Panel -->
            <Border Grid.Column="0" Background="#F5F5F5" CornerRadius="5" Padding="15" Margin="0,0,15,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="Select Job to Accept:" FontWeight="Bold" Margin="0,0,0,10"/>

                    <ComboBox Grid.Row="1" x:Name="JobsComboBox" 
                              ItemsSource="{Binding AvailableJobs}"
                              SelectedItem="{Binding SelectedJob}"
                              DisplayMemberPath="Name"
                              Margin="0,0,0,10"
                              Padding="5"/>

                    <TextBlock Grid.Row="2" Text="Job Details:" FontWeight="Bold" Margin="0,10,0,5"/>

                    <StackPanel Grid.Row="3" Margin="0,0,0,15">
                        <TextBlock Text="{Binding SelectedJob.Description}" TextWrapping="Wrap"/>
                        <TextBlock Text="{Binding SelectedJob.Site.SiteName}" Margin="0,5,0,0"/>
                        <TextBlock Text="{Binding SelectedJob.Client.Name}" Margin="0,5,0,0"/>
                    </StackPanel>

                    <Button Grid.Row="5" Content="Accept Job &amp; Create Equipment Batch" 
                            Command="{Binding AcceptJobCommand}"
                            Background="#4CAF50" Foreground="White"
                            Padding="10" FontWeight="Bold"/>
                </Grid>
            </Border>

            <!-- Equipment Requirements Panel -->
            <Border Grid.Column="1" Background="#F5F5F5" CornerRadius="5" Padding="15">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="Equipment Requirements:" FontWeight="Bold" Margin="0,0,0,10"/>

                    <DataGrid Grid.Row="1" x:Name="RequirementsDataGrid"
                              ItemsSource="{Binding JobRequirements}"
                              AutoGenerateColumns="False" IsReadOnly="True"
                              Margin="0,0,0,10">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Equipment Type" Binding="{Binding EquipmentType.Name}" Width="200"/>
                            <DataGridTextColumn Header="Planned Qty" Binding="{Binding PlannedQty}" Width="100"/>
                            <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="200"/>
                            <DataGridTextColumn Header="Priority" Binding="{Binding Priority}" Width="100"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <StackPanel Grid.Row="2" Orientation="Horizontal">
                        <Button Content="Add Requirement" Command="{Binding AddRequirementCommand}"
                                Background="#2196F3" Foreground="White" Padding="10" Margin="0,0,10,0"/>
                        <Button Content="Edit Requirement" Command="{Binding EditRequirementCommand}"
                                Background="#FF9800" Foreground="White" Padding="10" Margin="0,0,10,0"/>
                        <Button Content="Remove Requirement" Command="{Binding RemoveRequirementCommand}"
                                Background="#F44336" Foreground="White" Padding="10"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl> 