<UserControl x:Class="InfraScheduler.Database.Views.TechnicianView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             FontFamily="Segoe UI">

    <Grid Background="#FAFAFA">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="White" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1" Padding="24,16,24,16">
            <StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                    <TextBlock Text="Home" Foreground="#0078D4" FontSize="14" FontWeight="Normal"/>
                    <TextBlock Text=" > " Foreground="#666666" FontSize="14" Margin="8,0,8,0"/>
                    <TextBlock Text="Database" Foreground="#666666" FontSize="14" Margin="0,0,8,0"/>
                    <TextBlock Text=" > " Foreground="#666666" FontSize="14" Margin="0,0,8,0"/>
                    <TextBlock Text="Technicians" Foreground="#666666" FontSize="14" FontWeight="Normal"/>
                </StackPanel>
                
                <TextBlock Text="Technician Management" FontSize="32" FontWeight="Light" Foreground="#333333"/>
                <TextBlock Text="Manage technician profiles, skills, and certifications" FontSize="16" Foreground="#666666" Margin="0,8,0,0"/>
            </StackPanel>
        </Border>

        <!-- Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="24">
            <StackPanel>
                <!-- Search Bar -->
                <Border Background="White" CornerRadius="4" Padding="24" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="Search Technicians" FontSize="20" FontWeight="SemiBold" Foreground="#333333" Margin="0,0,0,16"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Search Name:" VerticalAlignment="Center" Margin="0,0,10,0" FontSize="14" FontWeight="Medium" Foreground="#333333"/>
                            <TextBox Grid.Column="1" Text="{Binding SearchTerm, UpdateSourceTrigger=PropertyChanged}" 
                                     Padding="12,8" VerticalContentAlignment="Center" Height="36"
                                     Background="White" BorderBrush="#E0E0E0" BorderThickness="1" FontSize="14"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Main Content -->
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="400"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Form Panel -->
                    <Border Grid.Column="0" Background="White" CornerRadius="4" Padding="24" Margin="0,0,16,0">
                        <StackPanel>
                            <TextBlock Text="Technician Details" FontSize="20" FontWeight="SemiBold" Foreground="#333333" Margin="0,0,0,16"/>
                            
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <TextBlock Text="First Name:" Grid.Row="0" Margin="0,0,0,5" FontSize="14" FontWeight="Medium" Foreground="#333333"/>
                                <TextBox Grid.Row="1" Text="{Binding FirstName, UpdateSourceTrigger=PropertyChanged}" 
                                         Margin="0,0,0,10" Padding="12,8" Height="36"
                                         Background="White" BorderBrush="#E0E0E0" BorderThickness="1" FontSize="14"/>

                                <TextBlock Text="Last Name:" Grid.Row="2" Margin="0,0,0,5" FontSize="14" FontWeight="Medium" Foreground="#333333"/>
                                <TextBox Grid.Row="3" Text="{Binding LastName, UpdateSourceTrigger=PropertyChanged}" 
                                         Margin="0,0,0,10" Padding="12,8" Height="36"
                                         Background="White" BorderBrush="#E0E0E0" BorderThickness="1" FontSize="14"/>

                                <TextBlock Text="Phone:" Grid.Row="4" Margin="0,0,0,5" FontSize="14" FontWeight="Medium" Foreground="#333333"/>
                                <TextBox Grid.Row="5" Text="{Binding Phone, UpdateSourceTrigger=PropertyChanged}" 
                                         Margin="0,0,0,10" Padding="12,8" Height="36"
                                         Background="White" BorderBrush="#E0E0E0" BorderThickness="1" FontSize="14"/>

                                <TextBlock Text="Email:" Grid.Row="6" Margin="0,0,0,5" FontSize="14" FontWeight="Medium" Foreground="#333333"/>
                                <TextBox Grid.Row="7" Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" 
                                         Margin="0,0,0,10" Padding="12,8" Height="36"
                                         Background="White" BorderBrush="#E0E0E0" BorderThickness="1" FontSize="14"/>

                                <StackPanel Grid.Row="9" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
                                    <Button Width="100" Content="Add" Margin="0,0,10,0" Command="{Binding SaveCommand}" 
                                            Padding="16,8" Height="36"
                                            Background="#0078D4" Foreground="White" BorderThickness="0" FontSize="14"/>
                                    <Button Width="100" Content="Update" Margin="0,0,10,0" Command="{Binding UpdateCommand}" 
                                            Padding="16,8" Height="36"
                                            Background="White" Foreground="#0078D4" BorderBrush="#0078D4" BorderThickness="1" FontSize="14"/>
                                    <Button Width="100" Content="Delete" Command="{Binding DeleteCommand}" 
                                            Padding="16,8" Height="36"
                                            Background="#E81123" Foreground="White" BorderThickness="0" FontSize="14"/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <!-- DataGrid -->
                    <Border Grid.Column="1" Background="White" CornerRadius="4" Padding="24">
                        <StackPanel>
                            <TextBlock Text="Technicians" FontSize="20" FontWeight="SemiBold" Foreground="#333333" Margin="0,0,0,16"/>
                            
                            <DataGrid ItemsSource="{Binding Technicians}"
                                      SelectedItem="{Binding SelectedTechnician, Mode=TwoWay}"
                                      AutoGenerateColumns="False" IsReadOnly="True"
                                      Background="White" BorderBrush="#E0E0E0" BorderThickness="1"
                                      GridLinesVisibility="Horizontal"
                                      HeadersVisibility="Column"
                                      CanUserAddRows="False"
                                      CanUserDeleteRows="False"
                                      SelectionMode="Single"
                                      MaxHeight="600">
                                <DataGrid.ColumnHeaderStyle>
                                    <Style TargetType="DataGridColumnHeader">
                                        <Setter Property="Background" Value="#F5F5F5"/>
                                        <Setter Property="Foreground" Value="#333333"/>
                                        <Setter Property="FontWeight" Value="SemiBold"/>
                                        <Setter Property="Padding" Value="12,8"/>
                                    </Style>
                                </DataGrid.ColumnHeaderStyle>
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="First Name" Binding="{Binding FirstName}" Width="120"/>
                                    <DataGridTextColumn Header="Last Name" Binding="{Binding LastName}" Width="120"/>
                                    <DataGridTextColumn Header="Phone" Binding="{Binding Phone}" Width="120"/>
                                    <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="200"/>
                                    <DataGridTextColumn Header="Skills" Binding="{Binding SkillsList}" Width="200"/>
                                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </StackPanel>
                    </Border>
                </Grid>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl> 