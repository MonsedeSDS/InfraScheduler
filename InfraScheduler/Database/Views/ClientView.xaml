<UserControl x:Class="InfraScheduler.Database.Views.ClientView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             FontFamily="Segoe UI">

    <Grid Background="#FAFAFA">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="White" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1" Padding="24,16,24,16">
            <StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                    <TextBlock Text="Home" Foreground="#0078D4" FontSize="14" FontWeight="Normal"/>
                    <TextBlock Text=" > " Foreground="#666666" FontSize="14" Margin="8,0,8,0"/>
                    <TextBlock Text="Database" Foreground="#666666" FontSize="14" Margin="0,0,8,0"/>
                    <TextBlock Text=" > " Foreground="#666666" FontSize="14" Margin="0,0,8,0"/>
                    <TextBlock Text="Clients" Foreground="#666666" FontSize="14" FontWeight="Normal"/>
                </StackPanel>
                
                <TextBlock Text="Client Management" FontSize="32" FontWeight="Light" Foreground="#333333"/>
                <TextBlock Text="Manage client information and contacts" FontSize="16" Foreground="#666666" Margin="0,8,0,0"/>
            </StackPanel>
        </Border>

        <!-- Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="24">
            <StackPanel>
                <!-- Form Section -->
                <Border Background="White" CornerRadius="4" Padding="24" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="Client Information" FontSize="20" FontWeight="SemiBold" Foreground="#333333" Margin="0,0,0,16"/>
                        
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Text="Name:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" FontSize="14" FontWeight="Medium" Foreground="#333333"/>
                            <TextBox Grid.Row="0" Grid.Column="1" Width="300" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" 
                                     Background="White" BorderBrush="#E0E0E0" BorderThickness="1" Padding="12,8" Height="36" Margin="0,4,0,12" FontSize="14"/>

                            <TextBlock Text="Email:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" FontSize="14" FontWeight="Medium" Foreground="#333333"/>
                            <TextBox Grid.Row="1" Grid.Column="1" Width="300" Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" 
                                     Background="White" BorderBrush="#E0E0E0" BorderThickness="1" Padding="12,8" Height="36" Margin="0,4,0,12" FontSize="14"/>

                            <TextBlock Text="Phone:" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" FontSize="14" FontWeight="Medium" Foreground="#333333"/>
                            <TextBox Grid.Row="2" Grid.Column="1" Width="300" Text="{Binding Phone, UpdateSourceTrigger=PropertyChanged}" 
                                     Background="White" BorderBrush="#E0E0E0" BorderThickness="1" Padding="12,8" Height="36" Margin="0,4,0,12" FontSize="14"/>

                            <TextBlock Text="Company:" Grid.Row="3" Grid.Column="0" VerticalAlignment="Center" FontSize="14" FontWeight="Medium" Foreground="#333333"/>
                            <TextBox Grid.Row="3" Grid.Column="1" Width="300" Text="{Binding Company, UpdateSourceTrigger=PropertyChanged}" 
                                     Background="White" BorderBrush="#E0E0E0" BorderThickness="1" Padding="12,8" Height="36" Margin="0,4,0,12" FontSize="14"/>

                            <TextBlock Text="Address:" Grid.Row="4" Grid.Column="0" VerticalAlignment="Top" FontSize="14" FontWeight="Medium" Foreground="#333333" Margin="0,8,0,0"/>
                            <TextBox Grid.Row="4" Grid.Column="1" Width="300" Text="{Binding Address, UpdateSourceTrigger=PropertyChanged}" 
                                     Background="White" BorderBrush="#E0E0E0" BorderThickness="1" Padding="12,8" Height="60" Margin="0,4,0,12" FontSize="14"
                                     TextWrapping="Wrap" AcceptsReturn="True"/>

                            <StackPanel Grid.Row="5" Grid.Column="1" Orientation="Horizontal" Margin="0,16,0,0">
                                <Button Width="100" Content="Add" Margin="0,0,10,0" Command="{Binding SaveCommand}" 
                                        Background="#0078D4" Foreground="White" Padding="16,8" BorderThickness="0" FontSize="14"/>
                                <Button Width="100" Content="Update" Margin="0,0,10,0" Command="{Binding UpdateCommand}" 
                                        Background="White" Foreground="#0078D4" Padding="16,8" BorderBrush="#0078D4" BorderThickness="1" FontSize="14"/>
                                <Button Width="100" Content="Delete" Command="{Binding DeleteCommand}" 
                                        Background="#E81123" Foreground="White" Padding="16,8" BorderThickness="0" FontSize="14"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Clients List -->
                <Border Background="White" CornerRadius="4" Padding="24">
                    <StackPanel>
                        <TextBlock Text="Clients" FontSize="20" FontWeight="SemiBold" Foreground="#333333" Margin="0,0,0,16"/>
                        
                        <DataGrid ItemsSource="{Binding Clients}"
                                  SelectedItem="{Binding SelectedClient, Mode=TwoWay}"
                                  AutoGenerateColumns="False" IsReadOnly="True"
                                  Background="White" BorderBrush="#E0E0E0" BorderThickness="1"
                                  GridLinesVisibility="Horizontal"
                                  HeadersVisibility="Column"
                                  CanUserAddRows="False"
                                  CanUserDeleteRows="False"
                                  SelectionMode="Single"
                                  MaxHeight="400">
                            <DataGrid.ColumnHeaderStyle>
                                <Style TargetType="DataGridColumnHeader">
                                    <Setter Property="Background" Value="#F5F5F5"/>
                                    <Setter Property="Foreground" Value="#333333"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                    <Setter Property="Padding" Value="12,8"/>
                                </Style>
                            </DataGrid.ColumnHeaderStyle>
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="150"/>
                                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="200"/>
                                <DataGridTextColumn Header="Phone" Binding="{Binding Phone}" Width="120"/>
                                <DataGridTextColumn Header="Company" Binding="{Binding Company}" Width="150"/>
                                <DataGridTextColumn Header="Address" Binding="{Binding Address}" Width="200"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl> 